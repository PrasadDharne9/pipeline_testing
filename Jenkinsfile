pipeline {
    agent any

    stages {
        stage('Run Python Script') {
            steps {
                script {
                    // Running the Python script directly
                    sh 'python Hello_world.py'
                }
            }
        }
    }
}